#!/bin/bash

# Check if docker compose services exist
if docker compose ps 2>/dev/null | grep -q mysql-test-container; then
    echo "The container 'mysql-test-container' exists."
    exit 0
else
    echo "The container 'mysql-test-container' does *not* exist."
    exit 1
fi
