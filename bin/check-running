#!/bin/bash

# Check if the container is running
if docker compose ps 2>/dev/null | grep mysql-test-container | grep -q Up; then
    echo "The container 'mysql-test-container' is running."
    exit 0
else
    echo "The container 'mysql-test-container' is *not* running."
    exit 1
fi
